- Use persistent HTTP connections
- Implement Server object
- Make client.rpc_foo(1, 2, 3) do the right thing
- De-hackify parsing code
- Implement <array>, <base64>, and <dateTime.iso8601> types
- Need to encode '<' and '&' in strings
- Implement via the 'rpc' package?
